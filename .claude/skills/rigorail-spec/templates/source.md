# Authoritative Product Source

This file preserves the informal product input **verbatim**. It is provenance,
not a generated specification.

Rules:

- never rewrite, summarise, or tidy the human's text into a cleaner brief;
- never mix a generated interpretation into a source block — interpretations
  belong in `product-spec.md` and `decisions.md`;
- append additional authoritative material as its own block rather than editing
  an existing one;
- reference each block from `decisions.md` as an `S-###` source.

## S-001 — <where this came from, e.g. "product draft pasted by the human">

> <original text, unedited>

## S-002 — <second authoritative source, if any>

Large or binary sources may be referenced instead of pasted:

- Path: `<path in this repository>`
- Retrieved: `<YYYY-MM-DD>`
- Authority: <what this source is allowed to establish>
